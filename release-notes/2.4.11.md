云母 Kirara 2.4.11 修复 HTTP 定时任务长期停留在「执行中」的问题。

- 修复执行结果保存时的数据库并发写冲突，失败时重试保存结果，不重复发送请求。
- 为任务执行增加整体超时和异常处理，避免异常执行长期占用任务。
- 自动回收超过请求超时加 60 秒仍未结束的执行记录，标记为中断、结果未知，后续计划可继续运行。
- 防止旧执行迟到的结果覆盖已恢复记录或误解锁新任务。

升级时保留原数据目录，重启后会将遗留的执行中记录标记为中断并恢复调度。Docker 正式版：`ghcr.io/imythu/kirara:2.4.11` / `latest`。
