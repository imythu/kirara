云母 Kirara 2.4.2 修复自动签到提交后过早读取页面、导致成功结果被记录为失败的问题。

- 修正 BrowserQL 条件步骤的执行顺序，确保点击或表单提交完成后再等待并读取签到结果。
- 保留已签到时跳过提交、图片验证码未填入时不提交的保护，并适配新的结果返回结构。

升级时保留原数据目录。Docker 正式版：`ghcr.io/imythu/kirara:2.4.2` / `latest`；测试渠道：`latest-beta`。
