云母 Kirara 2.4.4 完善保存路径分析的筛选流程，修正待下载空间和未完成种子统计。

- 支持统计前通过下拉框多选分类和标签；同组匹配任一项，分类与标签同时满足，不选则统计全部。
- 支持筛选未分类、无标签种子；修改筛选后清除旧结果，重新统计时读取最新种子数据。
- 使用实际剩余数据量计算待下载空间，修复辅种和重复下载导致的误判，并统一下载器概况与路径分析的计算依据。
- 种子明细显示实际完成数据量；尚未获取元数据的种子仍计入未完成数量。

升级时保留原数据目录。Docker 正式版：`ghcr.io/imythu/kirara:2.4.4` / `latest`；测试渠道：`latest-beta`。
