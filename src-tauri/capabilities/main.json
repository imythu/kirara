{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The main window may access the embedded API and its log stream.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-api-request",
    "allow-logs-open",
    "allow-logs-close"
  ]
}
