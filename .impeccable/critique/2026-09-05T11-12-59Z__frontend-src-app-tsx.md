---
target: 各级菜单
total_score: 26
max_score: 40
na_heuristics:
p0_count: 0
p1_count: 2
target_identity: "file:/workspace/rflush/frontend/src/App.tsx"
target_fingerprint: "sha256:f1732c3a65cccb294079e7f628b23969264e8ce6d36fb2381ca1a484b274f1c0"
target_path: /workspace/rflush/frontend/src/App.tsx
timestamp: 2026-09-05T11-12-59Z
slug: frontend-src-app-tsx
---
Method: dual-agent (A: /root/design_review · B: /root/detector_evidence)

各级菜单评审：26/40，需要明显改进。范围：主导航、手机底栏/抽屉、追剧与签到页签、设置分区及共用下拉选择器。依据：源码与 1440×900、390×844 模拟数据浏览器检查；未验证真实业务数据下的全部状态。

设计判断：紫色、图标和选中态一致，有温和的品牌辨识度；导航组织仍以功能清单为主，未充分体现“配置连接 → 找资源 → 自动执行 → 查看结果”的任务路径。最大机会是让用户始终知道入口在哪里、当前在哪、怎样回来。

| 启发式 | 分数 /4 | 主要依据 |
|---|---:|---|
| 系统状态可见 | 3 | 高亮明确，折叠后当前位置消失 |
| 符合现实认知 | 3 | 中文术语清楚，“自动追剧”实际还包含电影和手动搜索 |
| 用户控制与自由 | 2 | 可关闭弹层，但二级位置不进入历史 |
| 一致性与标准 | 2 | 局部页签成熟，全局导航语义缺口明显 |
| 错误预防 | 3 | 前置配置清单和危险操作确认有保护 |
| 识别而非记忆 | 2 | 手机无名图标、总览藏在品牌入口 |
| 灵活与效率 | 2 | 下拉和页签支持键盘，二级页面不可直达 |
| 美观与简约 | 3 | 品牌统一，菜单嵌套容器和说明文字偏多 |
| 错误恢复 | 3 | 分区失败有重试，部分全局错误缺少行动建议 |
| 帮助与文档 | 3 | 配置帮助就地提供，缺少任务型导航索引 |
| 合计 | 26/40 | Acceptable：需要明显改进 |

值得保留：桌面主导航的选中态清楚；追剧四页签稳定、手机两列布局和键盘语义完整；订阅前置配置清单、可搜索下拉及焦点恢复降低了操作负担。

优先问题：

1. P1 手机底栏缺少可识别的目的地名称。非选中项隐藏标签且没有 aria-label；手机日志按钮同样缺少替代名称。初次用户需要猜图标，读屏用户无法可靠辨认目的地。所有底栏项常显短标签，补齐按钮名称和当前页语义。位置：frontend/src/App.tsx:749、:556。建议 $impeccable harden。
2. P1 菜单抽屉的模态行为不完整。浏览器确认打开后焦点仍停留在背景“打开全部菜单”；源码未实现焦点圈定、初始定位与关闭恢复。复用现有 Dialog 的焦点机制，并给导航项添加 aria-current、折叠组添加 aria-expanded。Esc 和按钮键盘访问已经可用，应保留。位置：frontend/src/App.tsx:243、:510、:699。建议 $impeccable harden。
3. P2 主导航分类和总览入口不够明确。“PT 自动化”放了八个同级入口，说明文字频繁截断；“系统总览”只能点品牌进入，折叠组又隐藏当前子项。增加显式总览入口，并按资源订阅、连接配置、自动任务等任务关系组织，折叠后保留当前项提示；无需增加更多嵌套层级。位置：frontend/src/App.tsx:68、:393、:427、:699。建议 $impeccable shape。
4. P2 二级位置无法刷新、书签或历史返回。“资源搜索”“质量与设置”仍共用 #/media，mode 只在组件状态中；浏览器确认切换页签不改变 URL。将二级位置编码进 URL，保留必要查询状态。离开组件后的状态丢失判断来自源码，未声称完整往返实测。位置：frontend/src/pages/media-page.tsx:1054、:2130。建议 $impeccable harden。
5. P2 “配置质量配置”没有直达质量区域。“质量与设置”先排列媒体设置、OpenList，质量配置在其后；手机首屏尚未读完第一张卡片。增加媒体、质量、OpenList 的就地分区入口，前置配置链接直接定位对应区域。位置：frontend/src/pages/media-page.tsx:2184、:4544、:4604、:4616。建议 $impeccable layout。

认知负荷与情绪：八个同级入口本身不等于错误，但大分类没有帮助用户缩小搜索范围。初次配置清单让用户安心，进入长设置页寻找目标、手机猜图标以及返回丢失位置又消耗这种确定感。

角色风险：Jordan（新用户）容易在系统设置寻找 TMDB，或进入质量与设置后找不到质量配置；Alex（熟练用户）无法收藏或直达二级页面；Sam（键盘/读屏用户）遇到手机按钮无名、抽屉焦点和全局状态语义缺失。

次要观察：部分页面用长说明作主标题，手机会截断，应统一以模块名作为标题。分组标题 10px、说明 11px、当前底栏标签 9px 偏小。菜单不必逐项配截断的长说明，可把解释移到真正需要的地方。

检测证据：CLI 仅 1 条背景网格提示（frontend/src/index.css:29），对菜单影响较低。浏览器三页分别报告 10/14/13 个实例，不能合并当成 37 个独立问题。Inter 字体提示是字体栈启发式，不能证明中文字形实际使用 Inter；功能文字偏小有截图支持。检测在无头浏览器中成功注入，未提供用户可见覆盖层。

待讨论：先修复手机入口与键盘访问是否符合当前优先级？调整“PT 自动化”分类时，哪些现有分组或名称必须保留？
