# 云母

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

面向 PT 用户，帮助他们管理站点、下载器、影视订阅及自动化任务。界面以中文为主，支持桌面端和移动端浏览器。

普通用户与多站点资深用户之间的优先级尚未确定，后续设计不能擅自假定只服务其中一类。

## Product Purpose

将 PT 站点管理、多站资源搜索、影视订阅、下载与任务监控集中在一个 Web 工具中，减少重复配置与手动操作，并让自动化结果及失败原因可查询。

对外产品名为“云母”；英文项目名、仓库、二进制及 Docker 镜像统一为 `kirara`。新安装创建 `kirara.db`；迁移安装兼容已有 `rflush.db`。

## Operating Context

用户自行运行 Rust 服务或 Docker 容器，通过浏览器操作 React 界面。配置和运行状态保存在 SQLite 中。

首次使用主要流程为配置 PT 站点、连接 qBittorrent、设置 TMDB 凭据与质量方案，然后创建订阅或手动搜索资源。自动追剧与手动搜索复用站点、下载器、质量配置及持久化下载队列。

## Capabilities and Constraints

- 影视订阅：从 TMDB 搜索电影、电视剧及动漫，支持季集与动画绝对集目标、暂停恢复、手动扫描和查看扫描结果。
- 资源搜索：聚合 NexusPHP 与 M-Team 站点结果，按身份与质量规则匹配，展示候选信息、拒绝原因及站点错误。
- 下载：提交到 qBittorrent，使用持久化队列完成重试、去重及状态对账；手动覆盖匹配规则需要填写原因。
- PT 管理：站点账号数据、刷流任务、自动签到、Tracker 标签规则、下载器统计及系统日志。
- 种子转移：界面提供 qBittorrent 种子选择与 OpenList 转移进度跟踪。
- 质量配置提供内置方案与高级规则；破坏性重置需要清晰说明后果并保留现有确认流程。
- 仓库说明当前不内置用户认证，默认监听本机地址。部署到其他网络环境依赖外部访问限制及认证代理，不能在界面中暗示已有内置认证。
- 已保存的凭据不通过配置接口明文回传；更新时留空保留原值，明确清除才删除。
- 尚未实现的能力不得写成已支持，包括 scene/XEM 编号映射、每日剧日期编号、整季自动升级、更多下载器及通知渠道。

## Brand Commitments

保留“云母”对外名称、中文界面及现有业务术语。初始化未确定新的视觉风格、配色、字体或宣传主张。

## Evidence on Hand

- `README.md`：产品说明、配置流程、功能范围、部署方式及安全约束。
- `frontend/src/App.tsx`：导航、业务模块与页面入口。
- `frontend/src/pages/`：现有功能界面，包括种子转移、标签规则、自动签到与统计。
- `frontend/package.json`、`frontend/vite.config.ts`：React / TypeScript / Vite 前端，开发端口 5173，API 代理到本机 3000。

以上仓库现状经用户同意作为本次产品记录依据；本次初始化没有验证各项功能的运行结果。不得据此捏造用户数量、性能指标、客户背书或竞争优势。

## Product Principles

- 复用配置与工作流，减少重复维护站点和下载器信息。
- 自动化过程可解释，区分没有匹配资源、规则拒绝、站点错误和下载失败。
- 保留用户对自动化的控制，包括暂停、手动执行及有理由的单次覆盖。
- 明确展示破坏性操作的后果，保持凭据更新与清除语义一致。

## Open Decisions

- 普通用户与资深用户之间的优先级。
- 产品差异化定位及可验证的成功指标。
- 产品专属的无障碍、国际化或其他新增约束尚未确定。
